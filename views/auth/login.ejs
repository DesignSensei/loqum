<!-- views/auth/login.ejs -->

<% const view = loginView; %>

<div class="d-flex flex-center flex-column flex-lg-row-fluid">
  <div class="text-center mb-10">
    <img alt="Logo" src="/assets/images/logo_purple.png" class="h-30px h-lg-40px" />
  </div>

  <div class="d-flex flex-column flex-lg-row-fluid w-75 w-lg-500px p-10 order-1 order-lg-1">
    <div class="d-flex flex-center flex-column flex-lg-row-fluid">
      <form
        class="form w-100"
        novalidate="novalidate"
        id="kt_sign_in_form"
        action="/login"
        method="POST"
      >
        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />

        <% if (view.isInviteLogin) { %>
        <input type="hidden" name="inviteToken" value="<%= view.inviteToken %>" />
        <% } %>

        <!-- Heading -->
        <div class="text-center mb-11">
          <h1 class="text-gray-900 fw-bolder mb-3">
            <% if (view.isInviteLogin) { %> Accept Invite to
            <span class="brand-accent"><%= view.inviteBusinessName %></span> <% } else { %> Log In
            to <span class="brand-accent">Loqum</span>
            <% } %>
          </h1>

          <div class="text-gray-500 fw-semibold fs-6"><%= view.subtitle %></div>
        </div>

        <% if (view.isInviteLogin) { %> <% if (view.hasInviteError) { %>
        <div class="alert bg-light-danger border border-danger d-flex align-items-center p-4 mb-8">
          <i class="ki-duotone ki-information-5 fs-2x text-danger me-4">
            <span class="path1"></span>
            <span class="path2"></span>
            <span class="path3"></span>
          </i>

          <div class="d-flex flex-column">
            <span class="fw-bold text-gray-900 fs-6">Invite cannot be used</span>
            <span class="text-gray-600 fs-7"><%= view.inviteError %></span>
          </div>
        </div>
        <% } else { %>
        <div class="alert bg-light-primary border border-primary p-4 mb-8 text-center">
          <div class="fw-bold text-gray-900 fs-6 mb-1">Team Invite</div>

          <div class="text-gray-600 fs-7">
            You were invited to join <strong><%= view.inviteBusinessName %></strong> <% if
            (view.inviteBranchName) { %> at <strong><%= view.inviteBranchName %></strong> <% } %> as
            <strong><%= view.inviteRoleLabel %></strong>.
          </div>
        </div>
        <% } %> <% } %> <% if (!view.shouldDisableSubmit) { %> <% if (view.showGoogleLogin) { %>
        <!-- Google Login -->
        <div class="row g-3 mb-9">
          <div class="col-md-12">
            <a
              href="<%= view.googleLoginUrl || '/auth/google' %>"
              class="btn btn-flex btn-outline btn-text-gray-700 btn-active-color-primary bg-state-light flex-center text-nowrap w-100"
            >
              <img
                alt="Google Logo"
                src="/assets/media/svg/brand-logos/google-icon.svg"
                class="h-15px me-3"
              />

              <% if (view.isInviteLogin) { %> Sign in with Google to accept invite <% } else { %>
              Sign in with Google <% } %>
            </a>
          </div>
        </div>

        <div class="separator separator-content my-14">
          <span class="w-125px text-gray-500 fw-semibold fs-7">Or with email</span>
        </div>
        <% } %>

        <!-- Email -->
        <div class="fv-row mb-8">
          <% if (view.isInviteLogin) { %>
          <input
            type="email"
            placeholder="Email"
            name="email"
            value="<%= view.inviteEmail %>"
            autocomplete="off"
            class="form-control bg-transparent"
            readonly
            required
          />
          <% } else { %>
          <input
            type="email"
            placeholder="Email"
            name="email"
            autocomplete="off"
            class="form-control bg-transparent"
            required
          />
          <% } %>
        </div>

        <!-- Password -->
        <div class="fv-row mb-8" data-kt-password-meter="true">
          <div class="mb-1">
            <div class="position-relative mb-3">
              <input
                class="form-control bg-transparent"
                type="password"
                placeholder="Password"
                name="password"
                autocomplete="off"
                minlength="8"
                required
              />

              <span
                class="btn btn-sm btn-icon position-absolute translate-middle top-50 end-0 me-n2"
                data-kt-password-meter-control="visibility"
              >
                <i class="ki-duotone ki-eye-slash fs-2"></i>
                <i class="ki-duotone ki-eye fs-2 d-none"></i>
              </span>
            </div>
          </div>
        </div>

        <!-- Forgot Password -->
        <div class="d-flex flex-stack flex-wrap gap-3 fs-base fw-semibold mb-8">
          <div></div>
          <a href="/reset-password" class="link-primary">Forgot Password?</a>
        </div>

        <!-- Submit -->
        <div class="d-grid mb-10">
          <button type="submit" id="kt_sign_in_submit" class="btn btn-primary">
            <span class="indicator-label"><%= view.submitLabel %></span>

            <span class="indicator-progress">
              Please wait...
              <span class="spinner-border spinner-border-sm align-middle ms-2"></span>
            </span>
          </button>
        </div>
        <% } %>

        <!-- Signup -->
        <div class="text-gray-500 text-center fw-semibold fs-6">
          <% if (view.isInviteLogin) { %> Need to create an account?
          <a href="<%= view.signupUrl %>" class="link-primary">Sign up with this invite</a>
          <% } else { %> Not a user yet?
          <a href="<%= view.signupUrl %>" class="link-primary">Sign up</a>
          <% } %>
        </div>
      </form>
    </div>
  </div>
</div>
